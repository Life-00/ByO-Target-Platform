[project]
name = "ai"
version = "0.1.0"
description = "PubMedQA Validator Agent System"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "langgraph>=0.0.10",
    "pydantic>=2.0.0",
    "biopython>=1.80",
    "typing-extensions>=4.0.0",
]

[tool.setuptools]
packages = ["app"]
